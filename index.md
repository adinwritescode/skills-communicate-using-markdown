# Hello Github

###### (reps)

![AB, Moments before Disaster](https://i.ytimg.com/vi/YnPVsCuDy5s/maxresdefault.jpg)

```Rust
// Open in another tab
let mut search_state = SearchState::new(self, self.build_doctored_book(shared_books)?)?;

let calculation_function = calculate::build_calculation_function(
    self.fees_centi_bps.iter().copied(),
    self.token_decimals.iter().copied(),
    self.zero_for_ones.iter().copied(),
    self.cex_fee_bps,
    self.cex_side.is_buy(),
);
```
